<div class="form form-container">
  <p>ステータス</p>
  <!%= form_with(model: [offer, want], local: true) do |f| %>
  <div><%= hidden_field_tag :want_id, want.id %></div>
  <!%= f.submit "評価する", class: "btn btn-primary" %>
  <!% end %>
</div>
