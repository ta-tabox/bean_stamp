<% provide(:title, "コーヒー豆一覧") %>
<header class="header">
  <div class="h-full w-11/12 mx-auto flex justify-between items-end">
    <h1 class="page-title">コーヒー豆一覧</h1>
    <div class="">
      <%= link_to "新規作成", new_bean_path, class: "btn-primary" %>
    </div>
  </div>
</header>
<section class="mt-4">
  <% if @beans.any? %>
    <%= render @beans %>
    <%== pagy_nav_js(@pagy) %>
  <% end %>
</section>
<% content_for :aside do%>
  <% render partial: 'shared/user_aside' %>
<% end %>
