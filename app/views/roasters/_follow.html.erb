<%= form_with(model: current_user.roaster_relationships.build, local: true) do |f| %>
  <div><%= hidden_field_tag :roaster_id, @roaster.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>
