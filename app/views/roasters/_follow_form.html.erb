<% unless current_roaster?(@roaster) %>
  <div id="follow_form">
    <% if current_user.following_roaster?(@roaster) %>
      <%= render 'unfollow' %>
    <% else %>
      <%= render 'follow' %>
    <% end %>
  </div>
<% end %>
