<!DOCTYPE html>
<html lang="ja">
  <head>
    <title><%= yield(:title) %> | BeansApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta charset="utf-8" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body class="bg-gray-50">
    <div class="container max-w-screen-2xl md:mx-auto">
      <div class="flex flex-col justify-between md:flex-row">
        <%# ナビコンテンツ %>
        <div class="w-full md:w-28 md:min-h-screen md:flex-none">
          <%# モバイルトップナビ %>
          <div class="w-full md:hidden">
            <%= render 'layouts/mobile_top_nav' %>
          </div>
          <%# モバイルボトムナビ %>
          <div class="w-full md:hidden">
            <%= render 'layouts/mobile_bottom_nav' %>
          </div>
          <%# デスクトップサイドナビ %>
          <div class="h-full hidden md:block w-28">
            <%= render 'layouts/side_nav' %>
          </div>
        </div>
        <%# メインコンテンツ %>
        <main class="md:w-9/12 my-14">
          <div class="container mx-auto">
            <div class="w-full px-8">
              <div id="flash-messages">
                <%= render 'layouts/flash_messages' %>
              </div>
              <%= yield %>
            </div>
          </main>
          <%# サイドコンテンツ %>
          <aside class="hidden md:block w-3/12 px-4 border-l border-gray-200">
            <%= yield :aside %>
            アサイド
          </aside>
        </div>
      </div>
      <%# <%= debug(params) if Rails.env.development? %> %>
    </body>
  </html>
