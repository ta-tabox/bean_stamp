<!DOCTYPE html>
<html lang="ja">
  <head>
    <title><%= yield(:title) %> | BeansApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta charset="utf-8" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body class="bg-gray-50">
    <div class="container max-w-screen-2xl md:mx-auto border border-red-200">
      <div class="flex flex-col justify-between md:flex-row">
        <%# ナビコンテンツ %>
        <div class="w-full md:w-28 md:min-h-screen md:flex-none border border-red-200">
          <%# モバイルトップナビ %>
          <div class="w-full md:hidden">
            <%= render 'layouts/mobile_top_nav' %>
          </div>
          <%# モバイルボトムナビ %>
          <div class="w-full md:hidden">
            <%= render 'layouts/mobile_bottom_nav' %>
          </div>
          <%# デスクトップサイドナビ %>
          <div class="h-full hidden md:block w-28">
            <%= render 'layouts/side_nav' %>
          </div>
        </div>
        <!--
          <header class="h-28 border-b-2 border-red-200 py-15 flex items-center">
          <h1 class="ml-28"><strong><%= link_to 'Beans!', root_path %></strong></h1>
          <nav class="ml-5">
            <#% if user_signed_in? %>
              <#%= render 'layouts/signed_in_nav' %>
                <#% else %>
                  <#%= render 'layouts/signed_out_nav' %>
                    <#% end %>
                    </nav>
                  </header>
                  -->
        <%# メインコンテンツ %>
        <main class="md:w-9/12 my-14 border border-red-200">
          <div class="container mx-auto border border-red-200">
            <div class="w-full px-8">
              <div id="flash-messages">
                <%= render 'layouts/flash_messages' %>
              </div>
              <%= yield %>
              <div class="h-96 bg-green-300">aaa</div>
              <div class="h-96 bg-green-300">aaa</div>
              <div class="h-96 bg-green-300">aaa</div>
              <div class="h-96 bg-green-300">aaa</div>
              <div class="h-96 bg-green-300">aaa</div>
            </div>
          </div>
        </main>
        <%# サイドコンテンツ %>
        <aside class="hidden md:block w-3/12 px-4 border border-red-200 bg-red-200 bg-green-300">
          <%= yield :aside %>
          アサイド
        </aside>
      </div>
    </div>
    <%# <%= debug(params) if Rails.env.development? %> %>
  </body>
</html>
