<% provide(:title, "フォロー") %>
<h1 class="mb-1">フォロー</h1>
<hr class="mb-3">
<div class="container">
  <div class="w-200 pt-12 mx-auto">
    <% if @roasters.any? %>
      <ol>
        <%= render partial: 'roasters/roaster', collection: @roasters %>
      </ol>
      <%== pagy_nav_js(@pagy) %>
    <% end %>
  </div>
</div>
